# CLAUDE.md — <project>

## What this is
<2-3 sentences. What the project is, in plain words. What it is NOT.>

## Owner
<name> — <one line: stack, constraints, how they work>

## Canonicality
This repo is the source of truth. Agent memory is a pointer, never a copy.
If a file and memory disagree, the file wins — then update memory.

## Locked decisions
<If any decisions are settled, list WHERE they live (one file), not the
decisions themselves — copies desync.>

## Current phase
<One line: what's happening right now. Keep this current.>
